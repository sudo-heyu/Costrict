<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SafetybeltRegulator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/light_black</item>
    </style>

    <style name="Theme.SafetybeltRegulator" parent="Base.Theme.SafetybeltRegulator" />
</resources>