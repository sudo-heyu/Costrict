<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:strokeWidth="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- 左侧图标/头像区域 -->
        <ImageView
            android:id="@+id/avatar_icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/circle_indicator_gray"
            android:backgroundTint="#F0F0F0"
            android:padding="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/worker"
            app:tint="#555555" />

        <!-- 中间信息区域 -->
        <LinearLayout
            android:id="@+id/info_layout"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/status_action_layout"
            app:layout_constraintStart_toEndOf="@+id/avatar_icon"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:id="@+id/worker_name_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/white"
                android:textSize="18sp"
                android:textStyle="bold"
                tools:text="张三"/>

            <TextView
                android:id="@+id/worker_number_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textColor="@color/text_secondary"
                android:textSize="14sp"
                tools:text="工号: 12345" />
        </LinearLayout>

        <!-- 右侧动作和状态区域 -->
        <LinearLayout
            android:id="@+id/status_action_layout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <!-- 状态区域 -->
            <LinearLayout
                android:id="@+id/status_layout"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="end"
                android:orientation="vertical">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">
                    
                    <!-- 报警按钮：极致紧凑优化 -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/button_send_alert"
                        style="@style/Widget.Material3.Button.OutlinedButton"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginEnd="8dp"
                        android:minWidth="0dp"
                        android:minHeight="0dp"
                        android:paddingHorizontal="8dp"
                        android:paddingVertical="4dp"
                        android:insetTop="0dp"
                        android:insetBottom="0dp"
                        android:text="示警"
                        android:textColor="@color/status_danger"
                        android:textSize="12sp"
                        app:cornerRadius="16dp"
                        app:strokeColor="@color/status_danger"
                        app:strokeWidth="1dp"
                        app:icon="@android:drawable/stat_notify_error"
                        app:iconSize="12sp"
                        app:iconTint="@color/status_danger"
                        app:iconPadding="2dp"
                        app:iconGravity="textStart"
                        />

                    <com.google.android.material.chip.Chip
                        android:id="@+id/status_chip"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:clickable="false"
                        android:minHeight="32dp"
                        android:textColor="@color/white"
                        app:chipBackgroundColor="@color/status_normal"
                        app:chipCornerRadius="16dp"
                        app:chipMinHeight="32dp"
                        app:ensureMinTouchTargetSize="false"
                        app:textEndPadding="8dp"
                        app:textStartPadding="8dp"
                        tools:text="正常" />
                </LinearLayout>

                <TextView
                    android:id="@+id/last_updated_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:drawablePadding="4dp"
                    android:gravity="center_vertical"
                    android:textColor="@color/text_tertiary"
                    android:textSize="12sp"
                    app:drawableStartCompat="@android:drawable/ic_menu_recent_history"
                    app:drawableTint="@color/text_tertiary"
                    tools:text="00:15:30" />
            </LinearLayout>

        </LinearLayout>

        <!-- 兼容性保留 -->
        <View
            android:id="@+id/status_indicator"
            android:layout_width="1dp"
            android:layout_height="1dp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/status_text"
            android:layout_width="1dp"
            android:layout_height="1dp"
            android:visibility="gone" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>